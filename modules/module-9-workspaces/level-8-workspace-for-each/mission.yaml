concepts:
- for_each workspace config
- map keys
- workspace conditional
description: A `for_each` uses a map keyed by workspace names, but the `default` workspace
  is missing from the map.
difficulty: intermediate
expected_time: 12m
level: level-8-workspace-for-each
module: module-9-workspaces
name: Missing Workspace Key in Map
objective: Add the `default` key to the map so `terraform plan` succeeds in the default
  workspace.
xp: 200
