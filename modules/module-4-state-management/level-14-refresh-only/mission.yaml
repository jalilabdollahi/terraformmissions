concepts:
- -refresh-only
- state refresh
- output attributes
description: A `local_file` resource has an output that references `local_file.data.size`
  — an attribute that doesn't exist on `local_file`. Fix it so apply and `terraform
  apply -refresh-only` both work.
difficulty: intermediate
expected_time: 15m
level: level-14-refresh-only
module: module-4-state-management
name: Refresh Only
objective: Change the output to reference `local_file.data.filename` (a valid attribute)
  so the config applies and refresh-only mode works.
xp: 225
