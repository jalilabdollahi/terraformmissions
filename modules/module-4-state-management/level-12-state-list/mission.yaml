concepts:
- terraform state list
- reserved names
- resource naming
description: Five `random_string` resources are declared, but one is named `count`
  — a reserved Terraform meta-argument. This causes a validation error.
difficulty: intermediate
expected_time: 10m
level: level-12-state-list
module: module-4-state-management
name: State List and Naming
objective: Rename the `count` resource to a valid name (e.g., `epsilon`) so `terraform
  validate` passes.
xp: 175
